let username = prompt('Username');
let password = prompt('Password');

const pass = 'LoveIsHere';
const user = 'king2';

if(username === user && password === pass){
    document.write(user + ' welcome back!');
}